#include<bits/stdc++.h>
using namespace std;
map<string,int> mp;
int main(){
	while(true){
		string s;
		getline(cin,s);
		if(s.empty()){
			break;
		}
		cout<<s<<'\n';
	}
return 0;
}

